apiVersion: kustomize.config.k8s.io/v1beta1
configMapGenerator:
- behavior: merge
  literals:
  - kustomize.buildOptions="--enable-helm --enable-alpha-plugins"
  - |
    repository.credentials=- passwordSecret:
        key: git_token
        name: autopilot-secret
      url: https://github.com/
      usernameSecret:
        key: git_username
        name: autopilot-secret
  - |
    configManagementPlugins=- name: kustomize-build-with-helm
      init:
        command: ["/bin/sh", "-c"]
        args: ["helm dependency build"]
      generate:
        command: [ "sh", "-c" ]
        args: [ "kustomize build --enable-helm" ]
  name: argocd-cm
- behavior: merge
  literals:
  - server.insecure="true"
  name: argocd-cmd-params-cm
kind: Kustomization
namespace: argocd
resources:
- github.com/argoproj-labs/argocd-autopilot/manifests/base?ref=v0.4.17
# - resources/argocd-cm-cmp-kustomize-build-with-helm.yaml ###
# - resources/kustomize-with-helm.yaml
# patches:
# - path: exposeservice.yaml
# - path: kustomize-with-helm.sidecar.yaml
